"""DataVortex dashboard views.

Deliberately NOT named `pages/`: Streamlit auto-registers a `pages/` folder next to
the entry script as extra navigation, which would duplicate the sidebar with links
that render blank pages.
"""
