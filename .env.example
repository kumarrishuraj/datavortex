# DataVortex — no environment variables are required.
#
# The pipeline, dashboard, AI Investigator, tests and notebook all run without a .env
# file and without any API key. The AI Investigator's planner is rule-based and makes
# no network calls.
#
# If you add local settings later, copy this file to .env (which is gitignored).
# Never commit real keys or credentials.
